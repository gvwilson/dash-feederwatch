{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PyDebug current",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal"
        }
    ]
}